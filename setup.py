#!/usr/bin/env python

from distutils.core import setup

setup(name='Audiotranscode',
      version='0.1',
      description='python module to transcode between audio formats using the CLI frontends of the codecs already installed on your system, e.g lame, vorbis-tools etc',
      author='Pando85',
      author_email='pando855@gmail.com',
      url='https://github.com/pando85/audiotranscode',
      packages=['audiotranscode'],
     )